# Run XDG autostart things. By default don't run anything desktop-specific
# DESKTOP_ENV=""
# if which /usr/lib/openbox/xdg-autostart >/dev/null; then
#  /usr/lib/openbox/xdg-autostart $DESKTOP_ENV
# fi
( <% if @volume != 'undef' %>amixer sset 'Master' <%= @volume %>% &&<% end %> xrandr --output `xrandr | grep ' connected ' | sed -e 's/\ .*//'` --rotate <%= @rotation %> && mpv --loop=inf --saturation=<%= @saturation %> --contrast=<%= @contrast %> --brightness=<%= @brightness %> --no-osc <% if @video1_url %>/home/kiosk/video1.mp4<% end %> <% if @video2_url %>/home/kiosk/video2.mp4<% end %> ) &
